import assert from 'node:assert/strict';
import fs from 'node:fs';
import vm from 'node:vm';
import path from 'node:path';
import {fileURLToPath} from 'node:url';
const here=path.dirname(fileURLToPath(import.meta.url));
const source=fs.readFileSync(path.join(here,'../materials/liam-learning-app/science-geography-teaching-v4.js'),'utf8');
new vm.Script(source,{filename:'science-geography-teaching-v4.js'});
assert.match(source,/Population growth: linear vs\. exponential/);
assert.match(source,/Unchecked exponential growth forms a J-shaped curve/);
assert.match(source,/Carrying capacity is the number of organisms/);
assert.match(source,/Density-dependent factors become more influential as a population becomes crowded/);
assert.match(source,/Population growth: birthrate, death rate, and natural increase/);
assert.match(source,/Natural increase is birthrate minus death rate/);
assert.match(source,/Population distribution describes the pattern of where people live/);
assert.match(source,/Migration is the movement of people from place to place/);
assert.match(source,/Match each real chapter definition to its vocabulary term/);
assert.match(source,/Using only the lesson above/);
assert.match(source,/if\(S\.subject==='biology'&&Number\(ch\.number\)===4\)return bio4\(\)/);
assert.match(source,/if\(S\.subject==='geography'&&Number\(ch\.number\)===4\)return geo4\(\)/);
console.log('PASS: science/geography teaching layer parses and contains explicit Chapter 4 teaching, verified vocabulary, and block-aligned practice');
